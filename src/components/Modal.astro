---
const { id = "", title = "" } = Astro.props;
---

<dialog id={id} class="modal" closedby="any">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="text-md font-bold">{title}</h3>
    <div class="py-4">
      <slot />
    </div>
  </div>
</dialog>