---
const { id = "", title = "", i18n = "" } = Astro.props;
---

<dialog id={id} class="modal">
  <!-- Fake backdrop that closes the dialog -->
  <form method="dialog" class="modal-backdrop">
    <button>close</button> <!-- invisible area, just needs to be a dialog-closing control -->
  </form>

  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2 rtl:left-2 rtl:right-auto">✕</button>
    </form>
    <h3 class="text-md font-bold" data-i18n={i18n}>{title}</h3>
    <div class="pt-5">
      <slot />
    </div>
  </div>
</dialog>